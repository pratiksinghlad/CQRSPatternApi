# Copilot Agents Entry Point

This folder is a lightweight tool entry point only.

`AgentSkills/agents/` is the single source of truth for all agent definitions.
Load agents from `AgentSkills/agents/` based on the task.

Do not add or edit agent content under `.copilot/agents/`.
