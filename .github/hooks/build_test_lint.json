{
	"hooks": {
		"PostToolUse": [
			{
				"type": "command",
				"command": "dotnet build; if ($?) { dotnet test; if ($?) { dotnet format --verify-no-changes; if (-not $?) { dotnet format } } }"
			}
		]
	}
}